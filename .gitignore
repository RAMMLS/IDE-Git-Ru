# Папка сборки — здесь компилятор генерирует все артефакты.
# Она может весить гигабайты, её никогда не коммитят.
target/

# Файлы блокировки пакетного менеджера (если бы ты использовал его).
# Cargo.lock для приложений обычно коммитят, но если хочешь — добавь.
# Cargo.lock

# Временные файлы редакторов и ОС
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Файлы среды разработки (если используешь VS Code, но не хочешь папку .vscode)
# .vscode/